# Run C — 2026-09-22

**Gestart:** 05:19 CEST · **Bets gepubliceerd:** 0 · **Wedstrijden diep geanalyseerd:** 0 van 0
gekwalificeerd · **Looptijd:** 1,6 minuten

**Dit is de allereerste keer dat Run C draait.** `runs/` bevat tot vandaag geen enkel
`run-c`-bestand. De run staat op `main`, zoals §6a eist.

**Leesbaar dagrapport (§6c):** https://claude.ai/artifact/WveDVfb7yo7AAEF61fXjpo

## Stage -2 — takken

`git fetch --unshallow` was al gedaan door de sessie die vandaag Run A draaide; deze sessie heeft
dezelfde volledige geschiedenis. Gecontroleerd: alle vijftig remote-branches hebben 0 commits die
`main` niet al heeft (`git rev-list --count main..origin/<tak>` voor elke branch). Er viel niets te
mergen. `data/picks.jsonl` heeft 0 openstaande picks en `data/shadow.jsonl` 0 openstaande
schaduwpicks — Run A heeft dat vanochtend al gecontroleerd en afgewikkeld.

## Een infrastructuurgat, rechtgezet vóór de eerste analyse

`scripts/progress.py`, `shadow.py`, `calibration.py`, `ctxlog.py`, `toplist.py`, `report.py` en
`ledger.py` (stats) accepteerden voor `--run` alleen de keuzes `a`/`b`/`A`/`B` — geschreven toen er
alleen Run A en Run B bestonden. Run C kon daardoor geen van de verplichte §6-stappen draaien.

Nagegaan of dat een kale lijst-uitbreiding kon zijn of dat er ergens run-specifieke logica op
`a`/`b` hing: in geen van de zeven bestanden wordt `run` ergens anders gebruikt dan als label voor
bestandsnamen (`{date}-run-{run.lower()}.json`) en als waarde in de opgeslagen JSON
(`run.upper()`). Er zit geen `if run == "a"` of vergelijkbare vertakking in. De argparse-`choices`
zijn daarom uitgebreid met `c`/`C` in alle zeven bestanden, zonder verder iets aan het gedrag te
veranderen — Run A en Run B draaien op dezelfde code als gisteren.

## Dekkingsrapportage

| Competitie | Status | Toelichting |
|---|---|---|
| Vriendschappelijke interlands (INT, id 114) | GEEN WEDSTRIJD | doorlopend het hele jaar, maar niet vandaag |
| UEFA Nations League A (id 9806) | GEEN WEDSTRIJD | groepsfase begint 24 sep 2026 |
| UEFA Nations League B (id 9807) | GEEN WEDSTRIJD | groepsfase begint 24 sep 2026 |
| UEFA Nations League C (id 9808) | GEEN WEDSTRIJD | groepsfase begint 24 sep 2026 |
| UEFA Nations League D (id 9809) | GEEN WEDSTRIJD | groepsfase begint 24 sep 2026 |
| CONCACAF Nations League (id 9821) | GEEN WEDSTRIJD | groepsfase loopt sep/okt 2026, niet vandaag |
| CAF Afrika Cup-kwalificatie (id 10608) | GEEN WEDSTRIJD | speelt in dezelfde vensters als de Nations League |

**Afgekapt door `MAX_DEEP_ANALYSES`:** 0 — er was niets om te rangschikken (cap stond op 40,
dinsdag).

**Buiten datadekking:** 0 — de datadekkingspoort is er niet aan toegekomen, want de
competitiepoort (Stage 2) sloot alles al.

### Hoe dat is vastgesteld — de daglijst geeft groepen, geen competities (run-c.md, "Wat je moet
weten voordat je begint")

`fotmob.find_league(fx, "UEFA Nations League", "INT")` vindt niets, want Fotmob zet elke
Nations League-groep als eigen regel neer (bv. "UEFA Nations League A Grp. 2"). Daarom is de hele
daglijst van vandaag doorzocht op naam **en** op de bekende `primaryId`'s uit run-c.md in plaats
van op één exacte competitienaam:

```
python3 -c "from scripts import fotmob; from datetime import date
fx = fotmob.fetch_fixtures(date(2026,9,22))
[print(lg['primaryId'], lg['ccode'], lg['name'], len(lg['matches'])) for lg in fx['leagues']]"
```

Dat gaf **40 competities, 111 wedstrijden** — dezelfde daglijst die Run A vanochtend ophaalde.
Twee regels stonden op `ccode = INT`:

| primaryId | Naam | Duels |
|---|---|---|
| 9833 | Asian Games Grp. C / D | 3 |
| 9375 | Women's Champions League | 5 |

Geen van beide hoort op de runlijst. De Asian Games is een multisport-toernooi voor Olympische
landenteams (U23 met dispensatiespelers, net als de Spelen zelf — zie run-c.md, "Wat er níet op
staat") en de Women's Champions League is uitgesloten op naam (`Women`) én is bovendien een
clubtoernooi, geen landenteam. Geen van de zeven bekende `primaryId`'s uit run-c.md (114, 9806,
9807, 9808, 9809, 9821, 10608) kwam voor in de daglijst, en er stond ook geen regel waarvan de naam
met "Nations League", "Friendlies" of "AFCON"/"Africa Cup" begint. `GEEN WEDSTRIJD` is dus de
uitkomst van een positieve controle, niet van een lege zoekopdracht die niets vond.

**Expliciete controle op de uitsluitingslijst (run-c.md, "Wat er níet op staat"):** van de 40
competities in de daglijst bevat geen enkele naam `Women`/`W.` bij een landenteam-toernooi (de
Women's Champions League is een clubtoernooi en staat sowieso niet op de runlijst), en geen enkele
`U17`/`U19`/`U20`/`U21`/`U23`/`Olympic`. De Asian Games is uitgesloten op de leeftijdsregel van de
Spelen, niet op naam — dat staat hier expliciet omdat de naam zelf geen leeftijdscategorie noemt.

## Bronstatus deze run

Uitvoer van `python3 scripts/api_check.py`, letterlijk:

```
Controle van de databronnen die een sleutel nodig hebben.

=== The Odds API (odds per bookmaker) ===
  OK — 35 actieve voetbalcompetities beschikbaar.
  Quota: 18763 over, 1237 gebruikt deze maand.
  Relevante sportkeys:
    soccer_denmark_superliga
    soccer_efl_champ
    soccer_epl
    soccer_france_ligue_one
    soccer_germany_bundesliga
    soccer_germany_dfb_pokal
    soccer_germany_liga3
    soccer_italy_serie_a
    soccer_italy_serie_b
    soccer_netherlands_eredivisie
    soccer_spain_la_liga
    soccer_spain_segunda_division
    soccer_uefa_champs_league
    soccer_uefa_champs_league_women
    soccer_uefa_europa_conference_league
    soccer_uefa_europa_league
    soccer_uefa_nations_league

=== API-Football (statistieken) ===
  API_FOOTBALL_KEY niet gezet — overgeslagen.
  Zet hem in de omgeving van de geplande taak (zie README, 'Sleutels toevoegen').

=== Samenvatting ===
  odds (prijzen)        OK
  statistieken (kansen) niet beschikbaar

  Zonder een werkende statistiekenbron blijft de routine bets overslaan:
  zonder onafhankelijke kansinput zou 'my_prob' uit de odds zelf komen, en dat
  is precies wat _shared-rules.md §2 verbiedt. Dit is dus de sleutel die telt.
```

| Bron | Status | Detail |
|---|---|---|
| The Odds API | ok | `soccer_uefa_nations_league` staat al in de actieve lijst, ruim voor de start op 24 sep. 0 credits uitgegeven deze run — geen wedstrijd om prijzen voor te kopen. |
| API-Football | **key_missing** | `API_FOOTBALL_KEY` niet gezet. Onveranderd sinds 8 aug, geen nieuwe blokkade — voor Run C is dit sowieso niet de kansbron (zie hieronder). |
| Fotmob | ok | daglijst 22 sep opgehaald (40 competities, 111 duels); geen enkel verzoek gefaald. |
| `scripts/national.py` | ok | `data/national-fit.json`: 6784 interlands t/m 18 sep 2026, gefit 20 sep 2026. Niet opnieuw gebouwd deze run — er is geen interland gespeeld sinds de laatste `build`, dus het corpus is ongewijzigd (run-c.md: "draai build + fit aan het eind van elk interlandvenster, niet elke dag"). |
| `scripts/squadform.py` | ok | `data/national-squads.json`: 213 landen, gefetcht 21 sep 2026, 132 met bruikbare selectiewaarde (81 zonder, zoals gemeten in run-c.md). Eén dag oud — precies wat run-c.md aanraadt ("draai squadform.py build aan het begin van elk interlandvenster"). |

**Voor Run C is API_FOOTBALL_KEY sowieso niet de kansbron.** run-c.md wijst `scripts/national.py`
(landenrating, 0,70) en `scripts/squadform.py` (selectiewaarde, 0,30) aan als de twee methodes voor
interlands; die vervangen hier de xG/splits-combinatie van Run A en Run B. De ontbrekende sleutel
raakt dus alleen Run A en Run B (waar hij een derde, van Fotmob onafhankelijke bron zou zijn). Geen
nieuwe blokkade om te melden.

Wijzigingen t.o.v. vorige run: n.v.t. — eerste run van Run C.

## Wedstrijden

Geen. Nul wedstrijden in de runlijst, dus nul doorgerekend, nul markten bekeken en `scripts/
national.py` / `scripts/squadform.py` zijn deze run niet aangeroepen op een echt duel — alleen om
de status van hun corpora te controleren (hierboven).

`python3 scripts/progress.py verify --run C --date 2026-09-22`: *"Alle 0 geanalyseerde wedstrijd(en)
hebben alle zes markten gehad."* Groen, zoals het hoort bij nul wedstrijden.

## Topselectie

Leeg — er zijn geen gepubliceerde bets. `MAX_SHORTLIST` stond op 3 (dinsdag); er is niets om mee aan
te vullen.

`python3 scripts/toplist.py --run c --date 2026-09-22`: beide ranglijsten (met en zonder herijking)
leeg. `zonder_herijking` is ook leeg — geen enkele selectie is zelfs maar doorgerekend.

### Marktbalans

| Markt | Competities met prijzen | Selecties doorgerekend | Bets |
|---|---|---|---|
| 1X2 | 0 — geen wedstrijd | 0 | 0 |
| Asian Handicap · DNB · Double Chance | 0 — geen wedstrijd | 0 | 0 |
| Over/Under | 0 — geen wedstrijd | 0 | 0 |
| BTTS | 0 — geen wedstrijd | 0 | 0 |

De marktbalans-controle van §1a is vandaag niet van toepassing: er is niets ingekocht omdat er
niets te analyseren viel, niet omdat een plafond knelde.

### Net niet

Geen kandidaten. `progress.py verify` is groen op 0 wedstrijden — er is niets om als `near_miss`
vast te leggen.

## Afwikkeling vorige picks

`ledger.py open`: *"Geen picks klaar om af te wikkelen."* `shadow.py open`: *"Geen schaduwpicks
klaar om af te wikkelen."* Beide logboeken staan op 0 open — Run A heeft ze vanochtend al leeg
gemaakt, en Run C heeft zelf nog nooit een pick of schaduwpick gepubliceerd.

## Stand van het logboek

Deze logboeken zijn gedeeld met Run A en Run B; Run C heeft er vandaag niets aan toegevoegd of
veranderd. Ter referentie, ongewijzigd t.o.v. het runrapport van Run A van vanochtend:

`python3 scripts/ledger.py stats`:

```
ALLE PICKS
  picks            294  (0 open, 294 afgewikkeld, 51 void)
  hit rate         108/243 = 44.4%
  ROI (1u flat)    -17.46u over 243 bets = -7.2%
  gem. odds        2.27   gem. geclaimde edge +9.9 pp
  Brier eigen      0.2535
  Brier markt      0.2364   -> markt beter (verschil -0.0172)
  kalibratie       verwacht 140.5 winnaars, werkelijk 108 (-32.5)

data_tier = FULL   261 picks, 44.6%, ROI  -8.5%
data_tier = LIGHT   33 picks, 43.3%, ROI  +1.9%
alleen topselectie 162 picks, 46.2%, ROI  -7.8%
```

`python3 scripts/recalibrate.py show`: herijking op 2346 afgerekende gevallen t/m 2026-09-18,
`a=1.030, b=0.019` — het model zei gemiddeld 33.3%, werkelijk 33.3% (+0.0 pp). Niet van toepassing
op Run C: `recalibrate.py` is gefit op de clubvoetbal-populatie van Run A/B en Run C heeft nog geen
enkele afgerekende pick om zijn eigen fit op te baseren. Zolang dat zo is, loopt `my_prob` bij Run C
ongewijzigd door zonder herijking (§1g: "onder `MIN_OBSERVATIONS` wordt er niet herijkt") — een punt
om aan te houden zodra Run C wél picks begint te publiceren.

`python3 scripts/shadow.py stats`: 621 kandidaten, 597 afgewikkeld, hit rate 41,9%, ROI −7,5%. Geen
van deze rijen is van Run C.

`python3 scripts/calibration.py stats`: 1000 wedstrijden, 3000 uitkomsten, 31 dagen. Zelfde tabel als
in Run A's rapport van vandaag; Run C heeft nog geen `calibration`-blok bijgedragen.

`python3 scripts/ctxlog.py stats`: 794 afgewikkelde wedstrijden, 6 open — ongewijzigd, Run C heeft
vandaag geen context opgehaald.

`python3 scripts/margins.py stats`: 852 wedstrijden, 28 dagen — ongewijzigd.

## Bevinding

**Geen inhoudelijke bevinding vandaag — de enige "bevinding" is dat de infrastructuur nu bestaat.**
Run C had tot vandaag geen enkele run gedraaid, dus er is geen eigen logboek om iets uit af te lezen.
Wat wél is vastgesteld: de twee corpora waarop Run C draait (`national-fit.json`,
`national-squads.json`) zijn recent genoeg (20 en 21 sep) om zonder extra werk de NL-groepsfase van
24 sep te halen, en de `--run`-keuzelijst van zeven scripts miste `c`/`C` zonder dat daar
run-specifieke logica achter zat — beide zijn hierboven al beschreven en niet hier herhaald.

## Wat ik nog moet doen

- **24 sep 2026: de eerste echte kans voor Run C.** De UEFA Nations League-groepsfase begint dan,
  en dat is de eerstvolgende datum waarop deze runlijst wedstrijden kan bevatten. Tot die datum is
  `GEEN WEDSTRIJD` de juiste uitkomst voor elke dag, tenzij er een vriendschappelijke interland of
  een AFCON-kwalificatieduel tussenkomt — allebei doorlopend het hele jaar, dus in theorie ook
  vóór 24 sep mogelijk (vandaag niet, zoals hierboven gemeten).
- **Vlak vóór 24 sep `squadform.py build` opnieuw draaien** als de selecties dan meer dan een paar
  dagen oud zijn — bondscoaches maken hun selectie bekend in de week vóór de wedstrijden (run-c.md,
  punt 6 onder "Wat hier nog niet goed aan is"). Vandaag is de data één dag oud en dus nog prima,
  maar dat verandert tegen 24 sep.
- **Bij de eerste échte wedstrijd: `calibration`-blokken vastleggen per doorgerekend duel**, ook al
  levert dat geen bet op — dat is de enige manier waarop over een paar weken te zien is hoe de
  landenrating zich tot de markt verhoudt (run-c.md, "Wat het waard is, en wat niet").
- **Poort 7 op interlands loopt via de selectie, niet via de wedstrijdcontext** — `squadform.
  injuries()`, niet `context.fetch_match_context()`. Dat staat al in run-c.md maar is hier nog nooit
  in de praktijk gebruikt; de eerste echte wedstrijd is de eerste test.
- `API_FOOTBALL_KEY` blijft ontbreken voor Run A/B. Zie `README.md` → "Sleutels toevoegen". Niet van
  invloed op Run C.

---

> Beslissingsondersteuning, geen winnend systeem. Na de bookmakermarge is de verwachtingswaarde negatief.
