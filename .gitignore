__pycache__/
*.pyc
.DS_Store

# Team-xG per competitie, ververst elke dag door scripts/fotmob.py — geen bron van waarheid,
# alleen een snelheidswinst binnen één dag. Niet committen, wel elke run opnieuw opbouwen.
data/cache/

# Nooit committen: API-keys horen in de omgeving, niet in de repo
.env
*.key
secrets.json

# Ruwe tussenstap van de inter-competitiemeting (1,2 MB): alle Europese uitslagen plus alle
# binnenlandse standen, zoals `tmp-run/il_fetch.py` ze van Fotmob haalt. Regenereerbaar met dat
# script. Wat wél in de repo staat is `tmp-run/il_rows.json` — de 2111 rijen waarop daadwerkelijk
# is gefit — zodat de meting na te rekenen is zonder opnieuw 300 verzoeken te doen.
tmp-run/il_dataset.json
